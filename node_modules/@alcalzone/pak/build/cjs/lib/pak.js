"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var pak_exports = {};
__export(pak_exports, {
  detectPackageManager: () => detectPackageManager,
  packageManagers: () => packageManagers
});
module.exports = __toCommonJS(pak_exports);
var import_npm = require("./package-managers/npm/index.js");
var import_yarn_berry = require("./package-managers/yarn-berry/index.js");
var import_yarn_classic = require("./package-managers/yarn-classic/index.js");
const packageManagers = Object.freeze({
  yarn: import_yarn_berry.YarnBerry,
  yarnClassic: import_yarn_classic.YarnClassic,
  npm: import_npm.Npm
});
async function detectPackageManager({ cwd = process.cwd(), setCwdToPackageRoot = false, requireLockfile = true } = {}) {
  for (const factory of Object.values(packageManagers)) {
    const pm = new factory();
    pm.cwd = cwd;
    if (await pm.detect(true, setCwdToPackageRoot))
      return pm;
  }
  if (!requireLockfile) {
    for (const factory of Object.values(packageManagers)) {
      const pm = new factory();
      pm.cwd = cwd;
      if (await pm.detect(false, setCwdToPackageRoot))
        return pm;
    }
  }
  throw new Error("This directory tree does not contain a package.json");
}
__name(detectPackageManager, "detectPackageManager");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  detectPackageManager,
  packageManagers
});
//# sourceMappingURL=pak.js.map
